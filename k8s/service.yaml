apiVersion: v1
kind: Service
metadata:
  name: portfolio
  labels:
    app: portfolio
    app.kubernetes.io/name: portfolio
    app.kubernetes.io/instance: portfolio
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: portfolio
