---
import HomeLayout from '@/layouts/HomeLayout.astro';
import HomeTopbar from '@/components/home/HomeTopbar.astro';
import HomeHero from '@/components/home/HomeHero.astro';
import HomeAbout from '@/components/home/HomeAbout.astro';
import HomePipeline from '@/components/home/HomePipeline.astro';
import HomeProjectGrid from '@/components/home/HomeProjectGrid.astro';
import HomeNowDeploys from '@/components/home/HomeNowDeploys.astro';
import HomeBlog from '@/components/home/HomeBlog.astro';
import HomeContact from '@/components/home/HomeContact.astro';
import HomeFooter from '@/components/home/HomeFooter.astro';

import AsciiPortrait from '@/components/home/AsciiPortrait';
import HomeTicker from '@/components/home/HomeTicker';
import HomeInteractions from '@/components/home/HomeInteractions';
import CommandPalette from '@/components/home/CommandPalette';
import HomeTerminal from '@/components/home/HomeTerminal';
import TweaksPanel from '@/components/home/TweaksPanel';
import KonamiOverlay from '@/components/home/KonamiOverlay';

import { getCollection } from 'astro:content';

const allProjects = await getCollection('projects');
const featuredProjects = allProjects
  .sort((a, b) => (a.data.order ?? 99) - (b.data.order ?? 99))
  .slice(0, 3);

const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const recentPosts = allPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

// Site data passed to the terminal component
const siteData = {
  meta: {
    name: 'Dmitrii Malashikhin',
    role: 'Data Engineer',
    location: 'New York City',
    email: 'hello@malashikh.in',
  },
  about: {
    lead: "I'm a data / pipeline engineer with a cybersecurity background. Born in Russia, studied in Saint Petersburg, recently moved to New York City.",
    paras: [
      "Most of my work has been around building and operating data integrations end-to-end: researching sources, writing Python connectors, designing ETL, and enriching / validating security data.",
      "Off-hours: MMA, NBA, swimming, rollerblading, way too much music. Espresso tonic is my happy place.",
    ],
    facts: [
      { k: 'stack', v: 'Python · SQL · Airflow · K8s' },
      { k: 'fuel', v: 'Espresso tonic · sparkling water' },
    ],
  },
  now: {
    label: 'apr 2026',
    items: [
      { ic: '▶', k: 'listening', v: 'King Krule — Easy Easy', sub: 'on repeat this week' },
      { ic: '☕', k: 'drinking', v: 'Espresso tonic', sub: 'Café Grumpy, 3rd visit today' },
      { ic: '✎', k: 'reading', v: 'Designing Data-Intensive Apps', sub: 'ch. 7 — transactions' },
      { ic: '▲', k: 'building', v: 'a tiny SIEM normalizer', sub: 'weekend project · Python + DuckDB' },
    ],
  },
  projects: featuredProjects.map(p => ({
    year: String(new Date().getFullYear()),
    title: p.data.title,
    desc: p.data.description,
  })),
  contact: {
    links: [
      { label: 'hello@malashikh.in',   href: 'mailto:hello@malashikh.in',           ext: '✉' },
      { label: 'github/malashikhin',    href: 'https://github.com/malashikhin',      ext: '↗' },
      { label: 'linkedin/malashikhin',  href: 'https://linkedin.com/in/malashikhin', ext: '↗' },
    ],
  },
};

const tickerItems = [
  'currently in NYC 🗽',
  'reading: Designing Data-Intensive Apps',
  'espresso tonic — 3:1, no syrup',
  'open to interesting problems',
  'NVD sync at 09:00 UTC daily',
  'uptime 99.98% (last 90d)',
  'deploys from main, no staging drama',
  '40.7128°N · 74.0060°W',
  'digital minimalism since 2021',
  'MMA · NBA · swim · skate',
];
---

<HomeLayout>
  <!-- ── TOPBAR ── -->
  <HomeTopbar />

  <!-- ── HERO ── -->
  <section class="hero" id="top">
    <div class="sidemeta">LAT 40.7128° · LONG 74.0060° · PIPELINE NODE 04</div>
    <HomeHero />
    <AsciiPortrait client:load />
  </section>

  <!-- ── TICKER + PINBOARD ── -->
  <HomeTicker client:load items={tickerItems} />

  <!-- ── ABOUT ── -->
  <HomeAbout />

  <!-- ── PROJECTS ── -->
  <section class="block" id="projects">
    <div class="section-head">
      <div class="num">§ 02</div>
      <h2>Featured <span class="it">projects.</span></h2>
      <div class="aux">
        <a href="/projects" style="border-bottom:1px solid var(--accent)">view all →</a>
      </div>
    </div>

    <HomePipeline />
    <HomeProjectGrid projects={featuredProjects} />
    <HomeNowDeploys />
  </section>

  <!-- ── BLOG ── -->
  <HomeBlog posts={recentPosts} />

  <!-- ── CONTACT ── -->
  <HomeContact />

  <!-- ── FOOTER ── -->
  <HomeFooter />

  <!-- ── OVERLAYS & INTERACTIONS (React, client-side) ── -->
  <HomeInteractions client:load />
  <CommandPalette client:load />
  <HomeTerminal client:load siteData={siteData} />
  <TweaksPanel client:load />
  <KonamiOverlay client:load />

  <!-- ── KEYBOARD LEGEND ── -->
  <div class="kbd-legend" aria-hidden="true">
    <span><kbd>⌘K</kbd> palette</span><span class="sep">·</span>
    <span><kbd>J</kbd>/<kbd>K</kbd> sections</span><span class="sep">·</span>
    <span><kbd>:</kbd> terminal</span><span class="sep">·</span>
    <span><kbd>D</kbd> dark</span>
  </div>
</HomeLayout>
